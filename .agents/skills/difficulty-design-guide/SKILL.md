---
name: difficulty-design-guide
description: 坦克保卫战副本难度设计指南。规范难度梯度设计哲学，确保每次难度提升只改 2~3 条参数，做线性增量而非乘法叠加。在设计新副本难度、调整已有难度参数时使用。
---

# 坦克保卫战副本难度设计指南

## 核心原则

**每次难度提升只改 2~3 条参数，做 1.0 → 1.2 → 1.44 的线性增量。**

### 问题

旧版难度设计一次性改了 8~10 个维度（Boss 类型、场地、速度、弹药、掩体、道具种类/间隔、小虫类型/数量），多项乘法叠加导致难度陡升，easy 和 hard 完全不是同一个副本。

### 规则

1. 每个副本的 easy/normal/hard 之间，只允许 **2~3 个维度**有差异
2. 其余参数**全难度统一**（同一 Boss、同一武器、同一弹药量、同一道具配置等）
3. 优先用「**场地缩小 + 节奏加快**（间隔缩短/波次增多）」这两个维度，它们天然产生梯度且玩家直觉上能理解
4. 不要通过换 Boss 类型、换虫子类型、砍道具种类来提难度——这些是"换了一个副本"，不是"同一个副本更难了"

## 推荐的难度维度

### 首选维度（天然产生梯度）

| 维度 | 效果 | 玩家感知 |
|------|------|---------|
| 场地缩小 | 活动空间减少，躲避余地变小 | 直觉理解："地方小了" |
| 波次增多 | 需要存活更久 | 直觉理解："要撑更久" |
| 间隔缩短 | 出怪节奏加快 | 直觉理解："来得更密" |

### 次选维度（谨慎使用）

| 维度 | 效果 | 注意事项 |
|------|------|---------|
| 加新虫子类型 | 增加威胁多样性 | 只在 easy→normal 加一次，normal→hard 不再加 |
| 加精英波 | 周期性高难波次 | 只在 easy→normal 加一次 |
| 小虫召唤间隔缩短 | Boss 战中小虫压力增大 | 仍是"节奏加快"，可接受 |

### 禁止维度（这些是"换副本"）

| 维度 | 为什么禁止 |
|------|-----------|
| 换 Boss 类型 | 完全不同的战斗体验 |
| 换虫子类型（多次） | 每次换都是新体验 |
| 砍道具种类 | easy 有的道具 hard 没有 = 不公平 |
| 砍弹药量 | 同上 |
| 换武器 | 同上 |

## 完整示例

### 竞技场生存（arena_survival）

```lua
M.difficulty_settings = {
    easy = {
        half_size = 18,        -- 场地较大
        wave_count = 5,        -- 5 波
        has_spitter = false,   -- 无远程虫
        has_elite = false,     -- 无精英波
    },
    normal = {
        half_size = 14,        -- 场地缩小
        wave_count = 7,        -- 波次增多
        has_spitter = true,    -- 加远程虫
        has_elite = true,      -- 加精英波
    },
    hard = {
        half_size = 12,        -- 场地再缩小
        wave_count = 10,       -- 波次再增多
        has_spitter = true,    -- 同 normal（不再加新类型）
        has_elite = true,      -- 同 normal
    },
}
```

**难度梯度分析**：
- easy → normal：场地 18→14（-22%）+ 加 spitter + 加精英波 + 波次 5→7（+40%）→ **3 条改动**
- normal → hard：场地 14→12（-14%）+ 波次 7→10（+43%）→ **2 条改动**
- spitter/elite 只在 easy→normal 加一次，normal→hard 不再加

### Boss 讨伐（boss_hunt）

```lua
M.difficulty_settings = {
    easy = {
        half_size = 15,           -- 场地
        minion_interval = 0,      -- 不召小虫
    },
    normal = {
        half_size = 12,           -- 场地缩小
        minion_interval = 1800,   -- 30秒召小虫
    },
    hard = {
        half_size = 10,           -- 场地再缩小
        minion_interval = 1200,   -- 20秒召小虫
    },
}
```

**全难度统一**：Boss big-biter、速度 0.3、弹药 50、掩体 12、道具 12s/5 种、小虫 small

**难度梯度分析**：
- easy → normal：场地 15→12（-20%）+ 小虫间隔 0→30s → **2 条改动**
- normal → hard：场地 12→10（-17%）+ 小虫间隔 30s→20s（-33%）→ **2 条改动**
- Boss/弹药/掩体/道具全难度统一

## 难度设计检查流程

### 1. 列出所有参数

先列出副本的所有可调参数：

```
场地大小、波次数量、出怪间隔、Boss 类型、虫子类型、
弹药量、掩体数量、道具种类、道具间隔、时间限制、
精英波、小虫召唤间隔、小虫类型、...
```

### 2. 标记全难度统一参数

哪些参数在 easy/normal/hard 中**完全相同**：

```
Boss 类型 = big-biter（统一）
弹药量 = 50（统一）
掩体数量 = 12（统一）
道具种类 = 5 种（统一）
道具间隔 = 12s（统一）
小虫类型 = small-biter（统一）
```

### 3. 选择 2~3 个差异化维度

从首选维度中选择：

```
场地大小：easy=18, normal=14, hard=12
波次数量：easy=5, normal=7, hard=10
```

### 4. 验证梯度合理性

- 每级增幅是否在 20%~50% 之间？
- 是否有乘法叠加（如场地缩小 AND 波次增多 AND 出怪加快 = 三重叠加）？
- 玩家能否直觉理解"为什么 hard 更难"？

## 难度颜色体系

| 难度 | 颜色 | RGB | 用途 |
|------|------|-----|------|
| easy | 蓝色 | `{r=0.3, g=0.6, b=1.0}` | GUI 难度卡片 |
| normal | 紫色 | `{r=0.7, g=0.3, b=1.0}` | GUI 难度卡片 |
| hard | 橙色 | `{r=1.0, g=0.6, b=0.2}` | GUI 难度卡片 |

## 审查清单

设计副本难度时，对照检查：

- [ ] easy/normal/hard 之间只有 2~3 个维度有差异
- [ ] 其余参数全难度统一（列出统一参数清单）
- [ ] 优先使用场地缩小 + 节奏加快
- [ ] 没有换 Boss 类型 / 虫子类型 / 砍道具种类
- [ ] 每级增幅在 20%~50% 之间（线性增量）
- [ ] 新虫子类型/精英波只在 easy→normal 加一次
- [ ] 玩家能直觉理解"为什么更难"
- [ ] 难度颜色使用统一体系（蓝/紫/橙）

## 参考

- 竞技场生存：`maps/amap/instance/modules/arena_survival.lua`
- Boss 讨伐：`maps/amap/instance/modules/boss_hunt.lua`
- 副本框架：`maps/amap/instance/instance.lua`
- 项目核心规则：`CLAUDE.md`（副本难度设计哲学章节）
