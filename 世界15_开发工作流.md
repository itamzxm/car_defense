# 世界15 开发工作流框架

> 版本：v1.0（2026-07-24）
> 适用：世界15「塔防·四面楚歌」全部开发任务；框架本身可复用于后续世界。
> 上位规则：本框架服从 `CLAUDE.md` 全部约束（错误不可掩盖、改动已有代码先汇报、求助阈值、事件过滤器禁令、编码规则等）。

---

## 总流程图

```
阶段0 现状盘点 → 阶段1 计划确认（老板拍板）→ 阶段2 逐项开发循环 → 阶段3 收尾验收
                        ↑___________________________|
                        （开发中发现新需求/歧义 → 回到阶段1 补确认）
```

---

## 阶段0：现状盘点

对照当前代码实现逐条核对，把每个功能点标成四种状态之一：

| 状态 | 含义 |
|------|------|
| ✅ 已实现已验证 | 代码在 + 无头/实测通过 |
| 🟡 已实现未验证 | 代码在，但从未跑通测试 |
| 🔴 未实现 | 设计文档有、代码没有 |
| ❓ 待老板确认 | 需求本身有歧义或已变更 |

盘点结果写入 `世界15_开发计划.md`，作为计划的输入。

---

## 阶段1：开发计划确认（老板介入点 ①）

**核心目的：防止理解偏差和执行偏差。**

1. 每个开发项做成一张「需求卡」，格式固定：

```
### P-XX 功能名
- 需求原文：（设计文档/老板原话，一字不改）
- 我的理解：（AI 用自己的话复述，暴露理解偏差）
- 验收标准：（可判定的客观条件，逐条列出——这是"怎样算做完"的唯一依据）
- 涉及文件：（预计新增/修改的文件与位置）
- 疑问点：（有歧义的地方，明确列出选项让老板选）
- 状态：待确认 / 已确认 / 开发中 / 自测中 / 待人工验证 / ✅完成
```

2. **老板逐项拍板**：确认「我的理解」和「验收标准」无误后，状态改为「已确认」。
3. **未确认的项一律不开工。** 疑问点没答复前不允许按猜测实现。
4. 开发中途发现新歧义 → 停下该项，补一张需求卡回到本阶段，不擅自决策（与 CLAUDE.md「AI 自主决策规则」的边界：本工作流属于有拍板环节的模式，歧义必须上报）。

---

## 阶段2：逐项开发循环

每次只做一项（复杂项可拆子项），严格按以下步骤，**不允许跳步**：

### 2.1 探索
- 搜索已有代码，通读相关函数完整流程，确认复用/扩展哪个结构（CLAUDE.md「添加功能前必须先探索」）。
- 涉及修改已有代码 → 先向老板汇报文件、位置、内容、原因，批准后动手。

### 2.2 编码
- 含中文的 Lua 文件只用 edit 工具精确替换，禁止 Set-Content。
- 文本改动同步 zh-CN + en locale。
- 跨模块惰性调用走共享表注册（如 `WD.world15_xxx`），禁止 `rawget(package.loaded)`。

### 2.3 四层测试门禁（T1→T4 逐层通过）

| 层 | 名称 | 手段 | 能查什么 | 通过标准 |
|----|------|------|----------|----------|
| T1 | 静态检查 | 语法/块平衡校验（python 脚本或 luac） | 语法错、end 不配对 | 0 错误 |
| T2 | 无头加载 | `factorio.exe --start-server-load-scenario 保卫战场景` | require 路径、加载期运行时错 | 日志到 InGame 无 Error |
| T3 | 无头逻辑自测 | `selftest/factorio_selftest.py` + RCON 断言，结果写 `script-output/world15_selftest.json` | 纯逻辑：金币结算、科技锁、Boss 生成参数、商店定价、能量网数值等 | 全部断言 PASS |
| T4 | 人工进游戏验证 | 老板按清单实测 | 实时玩法：Boss 实际进攻、爆炸手感、GUI 交互、性能 | 清单逐项打勾 |

- **T1~T3 由 AI 全自动执行，T4 生成清单交给老板**（老板介入点 ②）。
- T3 测不了的项（需要真实实体/事件的玩法）必须明确写进 T4 清单，**不允许宣称"无头测过=没 bug"**。

### 2.4 Bug 修复循环

```
测试失败 → 定位根因（追根究底，禁止绕过/掩盖）→ 修复 → 重跑 T1~T3 全量回归
   ↑______________________________________________________|
```

- **回归原则：每次修复后重跑全部自测，不只跑出错那条**——修 A 弄坏 B 是本项目已发生过的真实事故模式。
- **求助阈值：同一问题修 2 次仍失败 → 停手，向老板汇报**（问题描述 + 文件路径 + 调用关系梳理），不许无限循环空转。
- 每个修掉的 bug 记入当日 memory（根因 + 修法），沉淀为回归断言加进 T3 套件。

### 2.5 单项收尾
- 全部测试通过 → 需求卡状态改「待人工验证」或「✅完成」。
- git 提交一次快照（见下方版本管理）。
- 追加当日工作记录。

---

## 阶段3：全部完成后收尾（老板介入点 ③）

1. **全量回归**：T1~T3 完整重跑一遍。
2. **收尾核查清单**：
   - [ ] locale zh-CN / en 全部同步
   - [ ] 测试探针/临时文件已清理（command_line.lua 挂载块、Temp 目录、探针场景）
   - [ ] 无违规事件过滤器（非 control.lua 的 Event.add 第三参数）
   - [ ] 无乱码（抽查含中文文件）
3. **交付物**：
   - `世界15_工作清单与成果.md`：所有完成项、每项的验收结果、修复的 bug 列表（根因+修法）、遗留问题、T4 人工验证总清单。
4. 老板按 T4 总清单进游戏做最终验收。

---

## 版本管理（快照与回滚）

- 场景目录 `git init`，`.gitignore` 排除日志/临时产物。
- 节奏：**每个开发项完成（过 T3）提交一次**；大改动开工前先提交当前状态。
- 目的：Set-Content 乱码类事故、修复引入新 bug 时可精确回滚，不再依赖手工备份目录。

---

## 铁律速查（测试相关，血泪教训）

1. 场景代码改完必须**新开一局**才生效（旧存档=旧代码）。
2. `/c` 控制台禁 `require`；访问已加载模块用共享表注册的接口。
3. 无头 `/c` 里 `print()` 不写日志，用 `log()` 或 `game.write_file()`。
4. RCON 响应分多包，客户端要持续 drain。
5. 五足虫命令用 `entity.commandable:set_command()`（2.0 接口），兜底 unit_group + `start_moving()`。
6. 游戏不在 nauvis！surface 一律取 `WD.get('surface_index')` / `this.active_surface_index` / `event.surface`。
7. 跳波测试：设 `wave_number = N-1` + `next_wave = game.tick - 1`。
